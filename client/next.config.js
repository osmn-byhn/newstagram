/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'img.freepik.com',
            'altinbilek.com.tr',
        ]
    }
}

module.exports = nextConfig
